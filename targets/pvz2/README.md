# PVZ2 Profile

This is an empty recognition interface. No package name is configured and no
PVZ2 input is read. `toolchain.ps1 target -Profile pvz2` must return a no-op
not-configured result and must not create a report.
